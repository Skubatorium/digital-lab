pages:
  stage: deploy
  image: alpine:3
  script:
    - mkdir -p public
    - cp showcase/index.html public/index.html
    - cp -r assets public/assets
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
